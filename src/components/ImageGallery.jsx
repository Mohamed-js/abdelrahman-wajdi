// import React from 'react';
// import { motion } from "framer-motion";

// const ImageGallery = ({ images, y }) => {
//     return (
//       <motion.div style={{ y }} className='flex gap-2 mt-2 '>
//         {images.map((imageUrl, index) => (
//           <div key={index}>
//             <img src={imageUrl} alt={`Image ${index}`} className="" />
//           </div>
//         ))}
//       </motion.div>
//     );
//   };
  

// export default ImageGallery;
